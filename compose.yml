services:
  db:
    image: postgres:16-alpine
    env_file:
      - .env
    volumes:
      - db_data:/var/lib/postgresql/data
  api:
    build: .
    env_file:
      - .env
    ports:
      - "3067:3000"

volumes:
  db_data: